using System;
using System.Collections.Generic;
using Entitas;
using UnityEngine;

//public sealed class StartEnemyWaveSystem : IReactiveSystem {
public class StartEnemyWaveSystem {// : ReactiveSystem {

    //protected StartEnemyWaveSystem(Context context) : base(context) { }

    //public void Execute(List<Entity> entities) {
    //    foreach(var e in entities) {
    //        var enemyViewController = (IEnemyController)e.view.controller;
    //        enemyViewController.InitWithWave(GameRandom.core.RandomInt(1, 3));
    //    }
    //}

    //protected override void Execute(List<Entity> entities) {
    //    foreach(var e in entities) {
    //        var enemyViewController = (IEnemyController)e.view.controller;
    //        enemyViewController.InitWithWave(GameRandom.core.RandomInt(1, 3));
    //    }
    //}

    //protected override bool Filter(Entity entity) {
    //    return entity.hasDebugMessage;
    //}

    //public TriggerOnEvent trigger { get { return Matcher.AllOf(CoreMatcher.View, CoreMatcher.Enemy).Added(); } }


    //protected override Collector GetTrigger(Context context) {
    //    return context.CreateCollector(Matcher.AllOf(CoreMatcher.View, CoreMatcher.Enemy));
    //}
}
